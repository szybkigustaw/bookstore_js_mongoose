class User {
  constructor(id, name, email, login, password) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.login = login;
    this.password = password;
  }
}

module.exports = User;
